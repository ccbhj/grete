(rule
  #:name "example rule"
  #:desc "find the prime number between 1-100"
  (when 
    (addr is-struct (eq @addressLine1 "nowhere"))
    (person is-struct (eq @name "Salaboy") )
  )
  (then
   (logf "Hey I just find %s that lives in %s" $person $addr))
)
